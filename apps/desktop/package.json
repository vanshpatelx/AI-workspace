{
  "name": "@ai-workspace/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'TODO: launch Electron + renderer dev server'",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ai-workspace/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
